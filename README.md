# BASE TANKS — Lite version

Lightweight version of Base Tanks (no wagmi heavy bundle). Uses Mini App SDK via CDN + native `window.ethereum`.

Recipient wallet (already configured): `0x9942E8725D2e46d2532CF24b61960e427E1F2589`
Domain (already configured): `base-tanks.vercel.app`

This version has only 3 npm dependencies (Next, React, ReactDOM) — should build in 30 seconds.
